on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'Folders Before'
        run: ls

      - uses: actions/checkout@v3

      - name: Folders After'
        run: ls
